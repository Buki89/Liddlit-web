query Post($id: Int!) {
  Post(id: $id) {
    id
    title
    text
    points
    voteStatus
    creatorId
    creator {
      id
      username
    }
    community {
      id
      name
    }
  }
}
